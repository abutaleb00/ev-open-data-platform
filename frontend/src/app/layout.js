import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "EV Data Hub | Open Data Operator Platform",
  description: "OCPI Compliant Smart Grid EV Charging Infrastructure Registry",
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      suppressHydrationWarning
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <head>
        {/* --- FREE OPENSTREETMAP LEAFLET MAP CSS DIRECTIVE --- */}
        {/* Guarantees cross-origin tile assets and pin graphics style beautifully out-of-the-box */}
        <link
          rel="stylesheet"
          href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
          integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY="
          crossOrigin=""
        />
      </head>
      <body className="min-h-full flex flex-col bg-[#F8FAFC]" suppressHydrationWarning>
        {children}
      </body>
    </html>
  );
}